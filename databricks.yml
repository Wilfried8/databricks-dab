# This is a Databricks asset bundle definition for dab_test_labs.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_test_labs
  uuid: eb1171b5-1d37-4ac1-a249-ccabec5f6d40

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-bc41e958-0cd5.cloud.databricks.com

  staging:
    mode: staging
    workspace:
      host: https://dbc-5af1a37f-9c4c.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/w43376199@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/w43376199@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: w43376199@gmail.com
        level: CAN_MANAGE

  prod:
    mode: production
    workspace:
      host: https://dbc-67cd619c-cb67.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/w43376199@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/w43376199@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: w43376199@gmail.com
        level: CAN_MANAGE
